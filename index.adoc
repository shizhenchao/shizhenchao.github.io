= Gold
:doctype: blog
:docinfo: shared
:author: zhenchao.shi
:email: zhenchao.shi@163.com
:revnumber: 1.0
:revdate: 2026-05-20
// :revremark: 
:title: 技术学习随笔博客
:subtitle: 学无先后，达者为师
:keywords: 编程,后端,技术笔记,学习心得
:description: 专注技术沉淀，记录编程实战与成长之路
:encoding: UTF-8
:lang: zh-CN
:toc: left
:toc-title: 目录
// :toclevels: 3
:sectnums: true
:source-highlighter: highlight.js
:icons: font
:table-frame: all
:admonition-icon: true

[quote, 荀子]
____
纸上得来终觉浅，绝知此事要躬行。
____


== Java 基础
== Java 面向对象
== Java 集合框架
== Java 多线程与并发
== Java IO 与 NIO
== Java 虚拟机（JVM）
== Java 设计模式
=== 什么是单例模式
单例模式确保一个类在整个应用中只存在一个实例，避免重复创建对象，节约资源并保证行为一致性。

=== 核心特点
* 构造方法私有（private）
* 自行创建实例
* 提供全局访问方法
* 线程安全

=== 适用场景
* 配置信息类
* 数据库连接池
* 线程池
* 日志工具类
* Spring Bean 默认作用域

=== 饿汉式
[source,java]
----
public class Singleton {
    private static final Singleton INSTANCE = new Singleton();
    private Singleton() {}
    public static Singleton getInstance() {
        return INSTANCE;
    }
}
----

=== 懒汉式（线程不安全）
[source,java]
----
public class Singleton {
    private static Singleton instance;
    private Singleton() {}
    public static Singleton getInstance() {
        if (instance == null) {
            instance = new Singleton();
        }
        return instance;
    }
}
----

=== 懒汉式（线程安全）
[source,java]
----
public class Singleton {
    private static Singleton instance;
    private Singleton() {}
    public static synchronized Singleton getInstance() {
        if (instance == null) {
            instance = new Singleton();
        }
        return instance;
    }
}
----

=== 双重检查锁 DCL
[source,java]
----
public class Singleton {
    private static volatile Singleton instance;
    private Singleton() {}
    public static Singleton getInstance() {
        if (instance == null) {
            synchronized (Singleton.class) {
                if (instance == null) {
                    instance = new Singleton();
                }
            }
        }
        return instance;
    }
}
----

=== 静态内部类单例
[source,java]
----
public class Singleton {
    private Singleton() {}
    private static class Holder {
        private static final Singleton INSTANCE = new Singleton();
    }
    public static Singleton getInstance() {
        return Holder.INSTANCE;
    }
}
----
== Java 算法与数据结构
== Java 面试题解析
== Java 项目实战
== Spring 核心原理
== Spring Boot 开发
== MyBatis 与持久层
== 微服务基础
== 代码规范与最佳实践